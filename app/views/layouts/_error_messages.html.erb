<% if model.errors.any? %>
  <div class="alert alert-warning" id="error_explanation">
    <h4><%= pluralize(model.errors.count, "error") %> 以下の原因を修正してください: </h4>
    <ul>
    <% model.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>
